"main";_250‍.e([["add",()=>add],["subtract",()=>subtract]]);function add (a, b) {
  return a + b;
}

function subtract (a, b) {
  return a - b;
}

// we can name a default export that will be used,
// if we don't declare anything specifically in the other file that requires imports
_250‍.d({
  name: 'bobo'
});